<div class="container">
  <h2 class="title-lg mb">Статьи</h2>

  <% @states.each do |state| %>
    <p class="mb-sm"><%= link_to "Вопрос #{state.id}", "/states/ #{state.id}" %></p>
  <% end %>

  <p class="mt"><%= link_to "Создать новый вопрос", new_state_path, class:'button button-main' %>
  </p>
</div>